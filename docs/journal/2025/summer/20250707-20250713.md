# 20250707-20250713

docker相关


1. docker将当前用户加入docker组

sudo usermod -aG docker $USER  # 将当前用户加入docker组


2. docker通过dockerfile打包镜像



1. 镜像上下文过大的问题

查找本项目下所有大文件

find . -type f -size +100M -exec ls -lh {} \+ | sort -k5 -rh


然后将其加入dockerignore中

再运行
docker build --no-cache -t test  . 2>&1 | grep "Sending build context"

查看上下文大小

发现大文件去掉之后上下文就很小了


cmake 3.17.5
gcc 4.8.5

先写周报

然后打包docker（已完成）


