# week17-20250106-20250112


https://blog.jywhy6.zone/2020/12/11/klee-notes/

符号执行引擎klee的用法


因为是在Ubuntu上做实验，直接通过snap的方式安装klee。


安装完了之后，运行klee发现已经可以正常运行，说明安装成功。

遇到的其他问题：

1. 头文件没有放进正确目录里面。

找到klee.h的文件位置，并且复制到/usr/include文件夹下。

2. 缺少一些tool，比如ktest-tool，以及klee-stats。

可以从github上clone下来，然后把这个二进制文件复制到/usr/bin目录下。

https://github.com/klee/klee



需要解决的问题：走http clone这个仓库报错，说设置的代理没有生效。但是我其实没有设置代理（？？


暂时走了ssh解决了。但是我真的没有设置http的代理。


可能是之前设置的全局？

解决方法应该是再次设置http的全局代理，把梯子搭起来。因为现在好像机器是用不了梯子的状态。



Cannot connect to the Docker daemon at unix:///var/run/docker.sock. Is the docker daemon running?

这种情况怎么办？

linux：

sudo systemctl start docker
sudo service docker start

mac：

打开docker desktop，点击启动docker

sudo brew services start docker


find 